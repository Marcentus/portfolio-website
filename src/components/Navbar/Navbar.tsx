import "./Navbar.css";

export default function Navbar() {
  return (
    <header>
      <nav>
        <a>Home</a>
        <a>Skills</a>
        <a>Projects</a>
        <a className="contact">Contact</a>
      </nav>
    </header>
  );
}
